# HackBite Backend Requirements
# Flask and related packages for web API
Flask==3.0.0
Flask-CORS==4.0.0

# Werkzeug for Flask utilities
Werkzeug==3.0.1

# Core Python packages (usually included but listed for completeness)
typing-extensions==4.8.0

# Optional packages for future features
requests==2.31.0  # For external API calls
python-dotenv==1.0.0  # For environment variable management

# PDF processing
PyPDF2==3.0.1  # For extracting text from PDF files

# GitHub scraping
beautifulsoup4==4.12.2  # For HTML parsing in GitHub scraper

# Development and testing packages (optional)
pytest==7.4.3  # For testing
pytest-flask==1.3.0  # Flask-specific testing utilities

# Note: SQLite3 is included with Python standard library
# No additional database drivers needed for SQLite